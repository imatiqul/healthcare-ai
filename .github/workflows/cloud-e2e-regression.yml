name: Cloud E2E Regression

# Runs the broader cloud regression and coverage suite after the live-seeded
# cloud smoke workflow passes, or on manual demand.
on:
  workflow_run:
    workflows:
      - "Cloud E2E Tests"
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      shell_url:
        description: 'Shell frontend base URL override (optional)'
        required: false
        default: ''
      voice_url:
        description: 'Voice MFE base URL override (optional)'
        required: false
        default: ''
      triage_url:
        description: 'Triage MFE base URL override (optional)'
        required: false
        default: ''
      scheduling_url:
        description: 'Scheduling MFE base URL override (optional)'
        required: false
        default: ''
      pophealth_url:
        description: 'Population Health MFE base URL override (optional)'
        required: false
        default: ''
      revenue_url:
        description: 'Revenue MFE base URL override (optional)'
        required: false
        default: ''
      encounters_url:
        description: 'Encounters MFE base URL override (optional)'
        required: false
        default: ''
      engagement_url:
        description: 'Engagement MFE base URL override (optional)'
        required: false
        default: ''
      gateway_aca_url:
        description: 'Gateway ACA base URL override (optional)'
        required: false
        default: ''
      api_base_url:
        description: 'Public API base URL override (optional)'
        required: false
        default: ''

permissions:
  contents: read

concurrency:
  group: cloud-e2e-regression-${{ github.event.workflow_run.head_sha || github.sha || github.run_id }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: frontend

env:
  # Resolution order: workflow_dispatch input -> repository variable.
  # No hardcoded production-like fallback endpoints are used.
  SHELL_URL: ${{ github.event.inputs.shell_url || vars.HEALTHQ_SHELL_URL || '' }}
  VOICE_URL: ${{ github.event.inputs.voice_url || vars.HEALTHQ_VOICE_URL || '' }}
  TRIAGE_URL: ${{ github.event.inputs.triage_url || vars.HEALTHQ_TRIAGE_URL || '' }}
  SCHEDULING_URL: ${{ github.event.inputs.scheduling_url || vars.HEALTHQ_SCHEDULING_URL || '' }}
  POPHEALTH_URL: ${{ github.event.inputs.pophealth_url || vars.HEALTHQ_POPHEALTH_URL || '' }}
  REVENUE_URL: ${{ github.event.inputs.revenue_url || vars.HEALTHQ_REVENUE_URL || '' }}
  ENCOUNTERS_URL: ${{ github.event.inputs.encounters_url || vars.HEALTHQ_ENCOUNTERS_URL || '' }}
  ENGAGEMENT_URL: ${{ github.event.inputs.engagement_url || vars.HEALTHQ_ENGAGEMENT_URL || '' }}
  GATEWAY_ACA_URL: ${{ github.event.inputs.gateway_aca_url || vars.HEALTHQ_GATEWAY_ACA_URL || '' }}
  API_BASE_URL: ${{ github.event.inputs.api_base_url || vars.HEALTHQ_API_BASE_URL || '' }}
  CLOUD_REGRESSION_SPECS: >-
    e2e-cloud/mfe-integration.spec.ts
    e2e-cloud/phase41-features.spec.ts
    e2e-cloud/platform-action-coverage.spec.ts
    e2e-cloud/platform-feature-coverage.spec.ts
    e2e-cloud/platform-journey-coverage.spec.ts
    e2e-cloud/post-deploy-regression.spec.ts
    e2e-cloud/shell-navigation.spec.ts
    e2e-cloud/workflow-handoff.spec.ts

jobs:
  cloud-e2e-gate:
    name: Cloud E2E Regression Gate
    runs-on: ubuntu-latest
    outputs:
      ready: ${{ steps.check.outputs.result }}
    steps:
      - name: Verify triggering Cloud E2E run executed live smoke
        id: check
        uses: actions/github-script@v9
        with:
          result-encoding: string
          script: |
            if (context.eventName !== 'workflow_run') {
              await core.summary
                .addHeading('Cloud E2E Regression Gate')
                .addRaw('Manual run detected - no upstream Cloud E2E smoke verification required.')
                .write();
              return 'true';
            }

            const runId = context.payload.workflow_run.id;
            const { owner, repo } = context.repo;
            const jobs = await github.paginate(github.rest.actions.listJobsForWorkflowRun, {
              owner,
              repo,
              run_id: runId,
              per_page: 100,
            });

            const smokeJob = jobs.find((job) => job.name === 'Smoke Gate (@smoke tests only)');
            const ready = smokeJob?.conclusion === 'success';

            await core.summary
              .addHeading('Cloud E2E Regression Gate')
              .addTable([
                [
                  { data: 'Job', header: true },
                  { data: 'Conclusion', header: true },
                  { data: 'Gate', header: true },
                ],
                [
                  { data: 'Smoke Gate (@smoke tests only)', header: false },
                  { data: smokeJob?.conclusion ?? 'missing', header: false },
                  { data: ready ? 'ready' : 'waiting', header: false },
                ],
              ])
              .write();

            if (!ready) {
              core.notice('Cloud E2E Regression will wait for a Cloud E2E Tests run where the Smoke Gate actually executed and passed.');
            }

            return ready ? 'true' : 'false';

  regression:
    needs: cloud-e2e-gate
    name: Cloud Regression Coverage Suite
    runs-on: ubuntu-latest
    timeout-minutes: 25
    if: needs.cloud-e2e-gate.outputs.ready == 'true'
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - uses: pnpm/action-setup@v6
        with:
          version: 9

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Validate endpoint configuration
        run: |
          MISSING=0
          for key in SHELL_URL VOICE_URL TRIAGE_URL SCHEDULING_URL POPHEALTH_URL REVENUE_URL ENCOUNTERS_URL ENGAGEMENT_URL GATEWAY_ACA_URL API_BASE_URL; do
            value="${!key}"
            if [ -z "$value" ]; then
              echo "::error::$key is required. Set the workflow input or repository variable."
              MISSING=1
              continue
            fi
            case "$value" in
              http://*|https://*) ;;
              *)
                echo "::error::$key must be an absolute http(s) URL (received: $value)"
                MISSING=1
                ;;
            esac
          done

          if [ "$MISSING" -ne 0 ]; then
            exit 1
          fi

      - name: Wait for shell SWA warmup
        run: |
          echo "Waiting for shell SWA to respond..."
          for i in $(seq 1 6); do
            STATUS=$(curl -s -o /dev/null -w '%{http_code}' "$SHELL_URL" || echo 000)
            echo "  Attempt $i: HTTP $STATUS"
            [ "$STATUS" = "200" ] && break
            sleep 10
          done
          echo "Warmup complete."

      - name: Run cloud regression coverage suite
        id: regression
        run: npx playwright test --config=playwright.cloud.config.ts $CLOUD_REGRESSION_SPECS --reporter=list,github,html,json,blob
        env:
          PLAYWRIGHT_JSON_OUTPUT_NAME: test-results/results.json

      - name: Capture regression endpoint diagnostics
        if: always()
        run: |
          mkdir -p artifacts/cloud-e2e
          DIAG_FILE="artifacts/cloud-e2e/regression-endpoint-matrix.md"

          {
            echo "# Regression Endpoint Diagnostics"
            echo ""
            echo "Generated: $(date -u +"%Y-%m-%dT%H:%M:%SZ")"
            echo "Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            echo ""
            echo "| Surface | URL | HTTP |"
            echo "|---|---|---|"
            for surface in SHELL VOICE TRIAGE SCHEDULING POPHEALTH REVENUE ENCOUNTERS ENGAGEMENT GATEWAY_ACA API_BASE; do
              URL_VAR="${surface}_URL"
              URL_VALUE="${!URL_VAR}"
              STATUS=$(curl -sS -m 15 -L -o /dev/null -w '%{http_code}' "$URL_VALUE" || echo ERR)
              echo "| ${surface,,} | $URL_VALUE | $STATUS |"
            done
          } > "$DIAG_FILE"

      - name: Write regression summary
        if: always()
        run: |
          echo "## Cloud Regression Coverage Suite" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          OUTCOME="${{ steps.regression.outcome }}"
          if [ "$OUTCOME" = "success" ]; then
            echo "All cloud regression coverage specs passed." >> $GITHUB_STEP_SUMMARY
          elif [ "$OUTCOME" = "failure" ]; then
            echo "Some cloud regression coverage specs failed - inspect the report artifacts below." >> $GITHUB_STEP_SUMMARY
          else
            echo "Cloud regression coverage was skipped or cancelled." >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "This workflow covers the broader cloud regression and mock-backed coverage specs that are intentionally excluded from the live-seeded Cloud E2E Tests workflow." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Diagnostics artifacts uploaded: endpoint matrix + Playwright raw output bundle (blob report + test-results)." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Trigger: ${{ github.event_name }} | Branch: ${{ github.event.workflow_run.head_branch || github.ref_name }} | SHA: ${{ github.event.workflow_run.head_sha || github.sha }}" >> $GITHUB_STEP_SUMMARY

      - name: Upload regression diagnostics bundle
        uses: actions/upload-artifact@v7
        if: ${{ always() && !cancelled() }}
        with:
          name: cloud-e2e-regression-diagnostics-${{ github.run_number }}
          path: |
            frontend/artifacts/cloud-e2e/regression-endpoint-matrix.md
            frontend/test-results/
            frontend/blob-report/
          if-no-files-found: warn
          retention-days: 14

      - name: Upload regression HTML report
        uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: cloud-e2e-regression-report-${{ github.run_number }}
          path: frontend/playwright-report/
          retention-days: 14

      - name: Upload regression JSON results
        uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: cloud-e2e-regression-results-json-${{ github.run_number }}
          path: frontend/test-results/results.json
          retention-days: 7
