apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: voice-service-policy
  namespace: healthcare
spec:
  podSelector:
    matchLabels:
      app: voice-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: ai-agent-service
        - podSelector:
            matchLabels:
              app: fhir-service
