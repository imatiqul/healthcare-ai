apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: pophealth-service-policy
  namespace: healthq-copilot
spec:
  podSelector:
    matchLabels:
      app: pophealth-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: fhir-service
        - podSelector:
            matchLabels:
              app: notification-service
