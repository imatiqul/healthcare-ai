apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: pophealth-service
  namespace: {{ .Values.global.namespace }}
spec:
  hosts:
    - "pophealth-service"
  http:
    - route:
        - destination:
            host: pophealth-service
            port:
              number: 8080
      retries:
        attempts: 3
        perTryTimeout: 2s
        retryOn: gateway-error,connect-failure,refused-stream
---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: pophealth-service
  namespace: {{ .Values.global.namespace }}
spec:
  host: pophealth-service
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
