<policies>
  <inbound>
    <base />
    <set-header name="X-Request-Id" exists-action="skip">
      <value>@(context.RequestId.ToString())</value>
    </set-header>
    <cors allow-credentials="true">
      <allowed-origins>
        <origin>{{ShellSwaUrl}}</origin>
        <origin>http://localhost:3000</origin>
      </allowed-origins>
      <allowed-methods preflight-result-max-age="600">
        <method>GET</method>
        <method>POST</method>
        <method>OPTIONS</method>
      </allowed-methods>
      <allowed-headers>
        <header>Content-Type</header>
        <header>Authorization</header>
        <header>X-Request-Id</header>
      </allowed-headers>
    </cors>
    <!-- Public demo endpoints: no JWT required -->
    <choose>
      <when condition="@(context.Operation.Id == "GetDemoSessions" || context.Operation.Id == "GenerateDemoInsights")">
        <!-- Admin-only demo endpoints require JWT -->
        <validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized" require-scheme="Bearer">
          <openid-config url="https://login.microsoftonline.com/{{TenantId}}/v2.0/.well-known/openid-configuration" />
          <audiences>
            <audience>api://healthq-copilot</audience>
          </audiences>
        </validate-jwt>
      </when>
      <!-- All other demo endpoints (start, next, feedback, complete, status) are public -->
    </choose>
    <!-- Stricter rate-limit for public demo endpoints to prevent abuse -->
    <rate-limit-by-key calls="5" renewal-period="30" counter-key="@(context.Request.IpAddress)" />
  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <base />
    <set-header name="X-Content-Type-Options" exists-action="override">
      <value>nosniff</value>
    </set-header>
    <set-header name="X-Frame-Options" exists-action="override">
      <value>DENY</value>
    </set-header>
  </outbound>
  <on-error>
    <base />
  </on-error>
</policies>
